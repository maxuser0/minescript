# Lines starting with "#" are ignored.

# To specify the location of your Python installation, uncomment the following
# line by deleting the "#" and changing the text within the double quotes to
# specify where Python is installed.
#python="/usr/bin/python3"
